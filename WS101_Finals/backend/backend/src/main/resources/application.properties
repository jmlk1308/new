spring.datasource.url=jdbc:mysql://localhost:3306/learning_hub?createDatabaseIfNotExist=true
spring.datasource.username=root
spring.datasource.password=081305

# CHANGE THIS LINE:
spring.jpa.database-platform=org.hibernate.dialect.MySQLDialect

# OR if you don't have that line, just ensure you are NOT forcing the old dialect.
# Often, you can even DELETE the dialect line entirely, and Spring Boot will auto-detect it.
# CHANGE THIS:


# TO THIS (Keeps your data safe):
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true

# File upload configuration
spring.servlet.multipart.max-file-size=100MB
spring.servlet.multipart.max-request-size=100MB